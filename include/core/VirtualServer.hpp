#pragma once

namespace core {

	class VirtualServer {
		public:
			VirtualServer();
			~VirtualServer();
			VirtualServer(const VirtualServer& other);
			VirtualServer& operator=(const VirtualServer& rhs);

		private:
	};

} /* namespace core */
