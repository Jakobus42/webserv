#pragma once

namespace http {

/**
 * @class AMessage
 * @brief ...
 */
class AMessage {
    public:
        AMessage();
        ~AMessage();
        AMessage(const AMessage &other);
        AMessage& operator=(const AMessage &rhs);

    private:

};

} /* namespace http */
